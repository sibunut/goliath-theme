# Goliath Theme
A simple theme for VS Code inspired by the One Monokai and Breeze themes, with a touch of coziness.

## How to build
Use `npm` to build.
```Bash
npm install -g vsce
vsce package
```